{
  "expo": {
    "name": "Hex",
    "slug": "hex-app",
    "version": "0.1.0",
    "orientation": "default",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "dark",
    "scheme": "hex",
    "newArchEnabled": true,
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.hex.app",
      "infoPlist": {
        "NSCameraUsageDescription": "Hex needs camera access to scan QR codes for daemon pairing.",
        "NSLocalNetworkUsageDescription": "Hex uses local network to discover relay daemons.",
        "NSBonjourServices": ["_hex._tcp."],
        "NSMicrophoneUsageDescription": "Hex uses the microphone for voice-to-text input.",
        "NSSpeechRecognitionUsageDescription": "Hex uses speech recognition to transcribe voice input."
      }
    },
    "android": {
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#1e1e2e"
      },
      "package": "com.hex.app"
    },
    "plugins": [
      "expo-router",
      "expo-secure-store",
      [
        "expo-camera",
        {
          "cameraPermission": "Hex needs camera access to scan QR codes for daemon pairing."
        }
      ],
      [
        "expo-image-picker",
        {
          "photosPermission": "Hex needs photo access to send images to agents."
        }
      ],
      [
        "expo-speech-recognition",
        {
          "microphonePermission": "Hex uses the microphone for voice-to-text input.",
          "speechRecognitionPermission": "Hex uses speech recognition to transcribe voice input."
        }
      ]
    ]
  }
}
